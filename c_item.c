#include <stdio.h>  
#include <string.h>  
#include <stdlib.h>

#define RECORD_MAX_SIZE 100010 
#define RECORD_MAX_LENGTH 1000

// define item 
// primary :W_ID
// define item.
struct item
{
        long I_ID;
        long I_IM_ID;
        char I_NAME[25+1];
        double I_PRICE;
        char I_DATA[50+1];
};

static int get_item(struct item **p_item_arr){ //?????????????????????????????????
	char file_name[] = "c_item.csv";  //?????????????????????????????????
    FILE *fp;  
    fp = fopen(file_name, "r");  
      
    if (!fp) {  
        fprintf(stderr, "failed to open file for reading\n");  
        return -1;  
    }
    
    char c;
    int i=0;
    int index=0;
    
    char **record_arr;
    record_arr=malloc(RECORD_MAX_SIZE*sizeof(char *));
    record_arr[index]=malloc(RECORD_MAX_LENGTH*sizeof(char));
    
    while(1){
      c=fgetc(fp);
      if(c==EOF){
        break;
      }
      if(c=='\n'){
        record_arr[index][i++]='\0';
        index++;
        record_arr[index]=malloc(RECORD_MAX_LENGTH*sizeof(char));
        i=0;
      }else{
        if(c!='"'){
          record_arr[index][i++]=c;
        }
      }
    }

    *p_item_arr=(struct item *)malloc(index*sizeof(struct item));//?????????????????????????????????
    
    for(i=1;i<index;i++){
      //printf("%s\n",record_arr[i]);
      char *token;
      
      token=strtok(record_arr[i],",");
      (*p_item_arr)[i].I_ID=atol(token);//?????????????????????????????????
      
      token=strtok(NULL, ",");
      (*p_item_arr)[i].I_IM_ID=atol(token);//?????????????????????????????????
      
      token=strtok(NULL, ",");
      strcpy((*p_item_arr)[i].I_NAME,token);

      
	    token=strtok(NULL, ",");
      (*p_item_arr)[i].I_PRICE=atof(token);//?????????????????????????????????
	  
      token=strtok(NULL, ",");
      strcpy((*p_item_arr)[i].I_DATA,token);
      
      token=strtok(NULL, ",");
      
      //printf("!!!!!!!!!!!!!!!!\n%ld\n%s\n%s\n%s\n%s\n%s\n%s\n%lf\n%lf\n",item_arr[i].W_ID,item_arr[i].W_NAME,
	  //		item_arr[i].W_STREET_1,item_arr[i].W_STREET_2,item_arr[i].W_CITY,item_arr[i].W_STATE,
	  //		item_arr[i].W_ZIP,item_arr[i].W_TAX,item_arr[i].W_YTD);
    }
     
    fclose (fp);  
    
    return index;
}

int main(int argc, const char * argv[]) {  
    
    struct item *item_arr;//?????????????????????????????????
    int index=get_item(&item_arr);//?????????????????????????????????
    int i;
    for(i=1;i<index;i++){ //?????????????????????????????????
		printf("!!!!!!!!!!!!!!!!\n%ld\n %ld\n %s\n %lf\n %s\n",
        item_arr[i].I_ID,
        item_arr[i].I_IM_ID,
        item_arr[i].I_NAME,
        item_arr[i].I_PRICE,
        item_arr[i].I_DATA);
    }
    return 0;  
}  
